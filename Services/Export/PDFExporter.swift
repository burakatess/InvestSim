import Foundation

final class PDFExporter {
    func export(summary: AssetSummary, assets: [AssetSnapshotDTO]) -> Data {
        // Mock implementation - in real app, this would generate PDF
        return Data()
    }
    
    func renderDashboardSnapshot(totalValue: String, roi: String) -> Data {
        // Mock implementation - in real app, this would generate PDF
        return Data()
    }
}